# ITER-02 T1h 及 T1 链条总验收（planner，2026-07-09）

## 结论：T1h 通过；T1–T1h 全链条关闭；四路对齐数据为项目首批可交付数据

planner 独立复验：dev_check 全绿（84 tests）；QA 样例 28 个文件全部合规（单图 <300KB）；gate JSON 与报告数字一致；**四路 t=30 抽帧目检——机位逐像素对齐、渲染差异符合预期（vanilla 平面水面 vs Complementary 云层/柔光/水面反射）、水池收于池沿之下、无黑边、无 toast**。

关键数字：
- 四路路线门：max 偏差 0.778–0.955 格（阈值 3.0），yaw 0.00003°，警告 0；
- 四路两两对齐：max 距离 0.216–0.475 格——"同轨迹不同画质"以半格内精度成立；
- 修复前的失败基线是 21–31 格。

## 排障链总账（P3–P10，全部 ITER-01 遗留、由路线门逐层暴露）

| # | 缺陷 | 修复 |
|---|---|---|
| P3 | replay 截断致 X server 按键残留跨 run 污染 | held 追踪 + stop_event + finally 释放 + 开局卫生释放 |
| P4 | 转向标定 540px/90°（差 10%） | 600px/90° + 钉死 sensitivity + 转向保真探针 |
| P5 | 行走 0.32s/格（实际 0.2316） | 实测标定 v/t0 + startup 补偿参数 |
| P6 | 岩浆源裸露漫流 | 玻璃盆围挡 |
| P7 | 场景固体未进 A* 障碍图 | 全量占用格审计 + 契约测试 |
| P8 | 场景清空 fill 超 32768 上限静默失败 | fill 拆分 + 构建回执校验（fail loud） |
| P9 | t_rel 基准比 replay 起点早 0.3s | replay 自记起点 |
| P10 | 水池与步行面同层无池沿、漫流淹平台 | 水池下沉一格 |

新增的永久防线：位置+朝向双路线门（ground truth）、场景构建回执校验、转向/行走标定探针、窗口几何记录。

## T1h 执行质量评（含风格/架构评估）

- 两个修复 commit 划分清晰；离屏窗口导致 ffmpeg 无效输入（`:77+-128,-72`）的边界情况被主动发现并防护（先移窗再回退），处理成熟。
- toast 定位到配方解锁 toast 并有 jar 层证据，敢于承认 `notificationDisplayTime:0.0` 超出文档范围并交由 review 裁决——**裁决：T4 里改回文档最小值 0.5**（真正的抑制来自 `inGameNotification:false`；越界值行为未定义，不留隐患），改完抽帧复验一次。
- 风格：与既有代码一致，类型完整，测试同 commit。无问题。
- 架构：pipeline.py 再度增长（R19 警告 2→3），完全在预期内——T4 架构清账的账期已到，立即执行。

## 下一步

1. coder → **T4 架构清账**（规格已在 PLAN；含 notificationDisplayTime 收尾）。
2. T3（17 路全矩阵）依赖用户提供 8 卡容器；T2 并行化代码与 shard 脚本已就绪。
3. T4 完成并 review 后，ITER-02 以"T3 待外部依赖"状态收口，ITER-03（程序化漫游 + scene.yml 统一 + 矩阵扩容）开启规划。
